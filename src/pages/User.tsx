import React from 'react';

import { Box, Typography } from '@mui/material';

const User: React.FC = () => {
  return (
    <Box>
      <Typography variant='h4'>User Profile</Typography>
      <Typography variant='body1'>This is the user profile page.</Typography>
    </Box>
  );
};

export default User;
